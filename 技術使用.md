# 專案技術棧與核心技術解析

## 專案總覽

本專案是一個功能豐富的 AI 英語學習平台，旨在透過尖端技術提供個人化、互動式的學習體驗。平台整合了教材問答、AI 口說對練、智能作文批改、以及創新的單字學習工具，其核心亮點在於深度整合了**檢索增強生成 (RAG)** 技術來提升內容的精準度與互動的智慧化。

---

## 核心技術深度解析

### 1. 檢索增強生成 (RAG) - 專案的技術基石

RAG 技術是本專案的靈魂，它巧妙地將外部知識庫與大型語言模型 (LLM) 的生成能力結合，確保了回答的準確性與時效性。專案中主要有兩種層次的 RAG 應用，這體現了開發者對該技術的深刻理解與靈活運用。

#### **應用場景一：標準 RAG 應用 - 教材問答系統 (`qa_system.py`)**

這是 RAG 技術的典型應用，為使用者提供一個能針對自訂教材提問的智慧問答機器人。

*   **技術流程**：
    1.  **數據預處理 (Indexing)** - `build_vector_db.py`:
        *   **文件載入**: 透過 `utils.py` 中的 `get_loader` 函式，支援多種格式（`.pdf`, `.csv`, `.txt`）的教材上傳。
        *   **文本切割**: 使用 LangChain 的 `RecursiveCharacterTextSplitter` 將長文件切成語意連貫的小文本塊 (Chunks)。
        *   **向量化 (Embedding)**: 調用 Google `GoogleGenerativeAIEmbeddings` 模型，將每個文本塊轉換為高維度向量。
        *   **存入向量資料庫**: 將生成的向量及對應的原始文本存儲在 `ChromaDB` 中，並為不同教材建立獨立的集合 (Collection)，實現數據隔離。
    2.  **查詢與生成 (Querying)** - `qa_system.py`:
        *   **問題向量化**: 當使用者提出問題時，同樣將問題文本轉換為向量。
        *   **相似度檢索**: 在 `ChromaDB` 中進行高效的向量相似度搜索，找出與問題最相關的幾個文本塊。
        *   **上下文增強**: 將檢索到的文本塊作為豐富的上下文 (Context)，與原始問題一同傳遞給 `GoogleGenerativeAI` 模型。
        *   **生成答案**: LLM 基於提供的精準上下文生成回答，有效避免了模型幻覺，並能根據 `README.md` 描述，提供資料來源。

*   **畢業報告亮點**:
    *   **端到端的自動化流程**: 從文件上傳到建立知識庫，再到查詢，整個流程高度自動化。
    *   **可擴展的知識庫**: 透過為每個文件建立獨立的 ChromaDB 集合，輕鬆實現多知識庫管理。

#### **應用場景二：進階 RAG 應用 - 智能單字提取 (`rag_vocabulary_extractor.py`)**

這是一個更具創意和技術深度的 RAG 應用，它利用 RAG 不僅是為了「回答問題」，更是為了「提煉資訊」。

*   **技術流程**:
    1.  **動態內存索引**: 當使用者貼上大段文字或上傳文章時，系統並非直接將全文丟給 LLM。而是在後端為這次請求**即時建立一個暫時性的、僅存在於記憶體中的 ChromaDB 向量索引**。
    2.  **上下文壓縮檢索 (Contextual Compression)**: 系統使用 LangChain 中更進階的 `ContextualCompressionRetriever`。它內部包含一個 `LLMChainExtractor`，會先用一個 LLM 任務去判斷向量索引中的哪些文本塊與「提取核心概念」這個目標最相關。
    3.  **精煉後再生成**: 檢索器返回的不再是原始的文本塊，而是經過第一輪 LLM 篩選和壓縮後的**精華內容**。
    4.  **精準指令提取**: 最後，將這些精華內容作為上下文，提交給第二個 LLM 鏈，並下達一個非常具體的指令：「請從這些核心內容中，提取出15個最重要、最值得學習的單字及其翻譯」。

*   **畢業報告亮點**:
    *   **創新的二階段 RAG**: 這不是單純的 `檢索 -> 生成`，而是 `檢索 -> 壓縮/提煉 -> 生成` 的二階段流程。這極大地降低了最終生成任務的噪音，讓 LLM 能更專注於從最相關的內容中提取單字，結果更精準。
    *   **效率與成本考量**: 相比將數千字的全文直接發送給 LLM，這種方法先在本地用向量檢索和輕量級 LLM 任務過濾資訊，顯著減少了最終生成任務的 Token 消耗，既省錢又高效。
    *   **無污染的臨時索引**: 使用內存向量資料庫，確保了每次提取都是獨立的，不會污染主知識庫，設計上非常乾淨。

### 2. AI 互動式口說練習 (`speaking_practice.py`)

此功能打造了一個高度互動的 AI 口說陪練，是另一個重要的技術亮點。

*   **核心技術**:
    *   **語音轉文字 (STT)**: 整合 `AssemblyAI` API，提供高準確度的語音識別。
    *   **對話記憶**: 在後端維護對話歷史，將上下文傳遞給 LLM，使 AI 能夠進行連貫的多輪對話。
    *   **深度評估系統**: Prompt Engineering 的精妙體現。精心設計的提示詞 (Prompt) 引導 LLM 不僅能對話，還能從語法、詞彙、流暢度、相關性四個維度對使用者的回答進行打分和提供具體回饋。
    *   **文字轉語音 (TTS)**: 使用 `gTTS` (Google Text-to-Speech) 將 AI 的回答轉換為語音，完成對話閉環。

### 3. 高可用 API 金鑰管理系統 (`api_manager.py`, `safe_gemini_llm.py`)

為了應對 LLM API 可能出現的速率限制或服務不穩，專案設計了一套輕量級但極其實用的 API 金鑰管理系統。

*   **核心技術**:
    *   **負載平衡**: `api_manager.py` 能夠管理一個 API 金鑰池，輪流使用不同的金鑰發送請求，分散負載。
    *   **自動故障轉移**: `safe_gemini_llm.py` 封裝了 Gemini 的調用。當偵測到一個金鑰失效（例如 API 錯誤或速率超限）時，它會自動從 `api_manager` 獲取下一個可用的金鑰重試，對使用者完全透明。
*   **畢業報告亮點**: 這是展現系統穩定性和工程實踐能力的絕佳範例，表明專案不僅是功能原型，還考慮了實際部署時的可靠性問題。

---

## 技術堆疊總覽

| 類別 | 技術/函式庫 | 說明 |
| :--- | :--- | :--- |
| **後端框架** | `Flask` | 輕量級的 Python Web 框架，負責路由和 API 端點。 |
| | `Flask-SQLAlchemy` | ORM (物件關聯對應)，用於操作 SQLite 資料庫。 |
| | `Flask-Login` | 處理使用者身份驗證和會話管理。 |
| **資料庫** | `SQLite` | 儲存使用者資料、學習記錄、單字本等結構化數據。 |
| | `ChromaDB` | 核心向量資料庫，用於 RAG 的高效相似度檢索。 |
| **AI / LLM** | `google-generativeai` | 直接調用 Google Gemini 系列模型的官方 SDK。 |
| | `langchain` | 核心 AI 框架，用於構建和編排 RAG 及其他 LLM 應用鏈。 |
| | `langchain-google-genai`| LangChain 與 Google GenAI 的整合套件。 |
| **語音處理** | `assemblyai` | 用於高質量的語音轉文字 (STT)。 |
| | `gTTS` | 用於將文字轉換為語音 (TTS)。 |
| | `pydub`, `soundfile` | 用於後端對音訊檔案的處理與操作。 |
| **數據處理** | `pandas` | 用於讀取和處理 `.csv` 格式的教材檔案。 |
| **前端** | `HTML5`, `CSS3`, `JavaScript` | 標準前端三件套，建構使用者互動介面。 |
| **開發工具** | `pyngrok` | 將本地開發的 Web 服務安全地暴露到公網，方便測試。 |
