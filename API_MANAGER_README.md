# 🚀 輕量化多 API 協作管理器

## 📋 功能特色

✅ **多金鑰負載平衡** - 自動在多個 API 金鑰間分配請求  
✅ **智能錯誤重試** - 自動重試失敗的請求並切換金鑰  
✅ **金鑰隱藏保護** - API 金鑰存儲在配置文件中，不暴露在代碼裡  
✅ **實時監控** - 管理員後台可查看 API 使用統計  
✅ **零侵入整合** - 無需修改現有業務邏輯代碼  
✅ **自動故障恢復** - 失效金鑰會自動重新啟用  

## 🔧 快速設置

### 1. 配置 API 金鑰
編輯 `.env` 文件：
```json
{
  "api_keys": [
    "你的第一個Gemini API金鑰",
    "你的第二個Gemini API金鑰", 
    "你的第三個Gemini API金鑰"
  ],
  "settings": {
    "max_retries": 3,
    "retry_delay": 1,
    "failure_threshold": 3,
    "reset_interval_hours": 1
  }
}
```

### 2. 安裝依賴
```bash
# 已包含在現有的 requirements.txt 中
pip install google-generativeai
```

### 3. 啟動應用
```bash
python app.py
```

## 📊 監控功能

### 管理員後台監控
- 訪問 `/admin/dashboard` 查看 API 管理器狀態
- 實時顯示：
  - 可用金鑰數量
  - 總請求數和成功率
  - 每個金鑰的詳細統計
  - 平均響應時間

### API 端點
- `GET /api/manager/health` - 健康檢查
- `GET /api/manager/stats` - 獲取統計信息（需管理員權限）

## 🔄 工作原理

### 負載平衡策略
- **輪詢分配**: 選擇使用次數最少的金鑰
- **故障隔離**: 連續失敗3次的金鑰會被暫時禁用
- **自動恢復**: 每小時重置失效金鑰狀態

### 錯誤處理機制
1. 請求失敗時自動切換到下一個可用金鑰
2. 最多重試3次
3. 記錄失敗統計，用於故障分析

### 統計追蹤
- 每個金鑰的請求次數
- 成功率和失敗率
- 平均響應時間
- 連續失敗次數

## 📈 使用統計

### 啟動時顯示
```
🔧 API 管理器已啟動
📊 可用金鑰: 3/3
📈 總請求數: 0
✅ 成功率: 0.0%
```

### 管理員後台顯示
- **API 服務概況**: 可用金鑰、總請求數、成功率、失效金鑰
- **金鑰詳細狀態**: 每個金鑰的使用情況和性能指標

## 🛠️ 進階配置

### 環境變數配置
可以通過環境變數設置 API 金鑰：
```bash
export GEMINI_API_KEY_1="你的第一個金鑰"
export GEMINI_API_KEY_2="你的第二個金鑰"
export GEMINI_API_KEY_3="你的第三個金鑰"
```

### 自定義設置
在 `api_config.json` 中調整：
- `max_retries`: 最大重試次數
- `retry_delay`: 重試延遲時間（秒）
- `failure_threshold`: 金鑰失效閾值
- `reset_interval_hours`: 重置間隔（小時）

## 🔒 安全特性

### API 金鑰保護
- 金鑰存儲在配置文件中，不在代碼中硬編碼
- 日誌中只顯示金鑰後8位，保護完整金鑰
- 支援環境變數配置，適合容器化部署

### 訪問控制
- 統計信息僅管理員可查看
- 健康檢查端點公開，便於監控

## 🚨 故障排除

### 常見問題

**Q: 所有金鑰都失效了怎麼辦？**
A: 系統會每小時自動重置失效狀態，或者重啟應用程式。

**Q: 如何添加新的 API 金鑰？**
A: 編輯 `api_config.json` 文件，添加新金鑰後重啟應用。

**Q: 為什麼成功率很低？**
A: 檢查金鑰是否有效，是否達到配額限制，或網路連接問題。

### 日誌查看
```bash
# 查看應用日誌
tail -f app.log

# 檢查 API 管理器狀態
curl http://localhost:8000/api/manager/health
```

## 📝 更新日誌

### v1.0.0 (2025-01-10)
- ✅ 初始版本發布
- ✅ 多金鑰負載平衡
- ✅ 智能錯誤重試
- ✅ 實時監控面板
- ✅ 管理員後台整合

## 🤝 技術支援

如果遇到問題：
1. 檢查 `api_config.json` 配置是否正確
2. 確認 API 金鑰有效性
3. 查看管理員後台的統計信息
4. 檢查應用日誌

---

**🎉 享受更穩定、更智能的 API 服務！**