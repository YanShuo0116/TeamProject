# 測驗狀態追蹤功能更新

## 問題描述

1. **測驗途中閃退會顯示已完成**：當使用者在進行單字測驗過程中意外離開或刷新頁面時，系統會錯誤地將該次測驗標記為已完成狀態。
2. **主題頁面缺少狀態說明**：課次選擇頁面沒有顏色代碼說明，使用者不清楚黃色和綠色框框的含義。

## 解決方案

### 1. 數據庫結構修改

- 在 `QuizAttempt` 模型中添加 `status` 字段，支持以下狀態：
  - `in_progress`：測驗進行中
  - `completed`：測驗已完成
  - `abandoned`：測驗被放棄（閃退或刷新頁面）

### 2. 後端API改進

#### 新增API端點
- `GET /api/quiz_status`：檢查特定課程的測驗狀態

#### 修改現有API
- `POST /api/start_quiz`：
  - 檢查是否有進行中的測驗
  - 將之前未完成的測驗標記為 `abandoned`
  - 創建新測驗時設置狀態為 `in_progress`

- `POST /api/complete_quiz/<quiz_id>`：
  - 驗證測驗狀態必須為 `in_progress`
  - 完成時設置狀態為 `completed`

#### 課程進度邏輯修改
- 移除自動標記課程完成的邏輯
- 只有通過測驗才能標記課程為完成狀態

### 3. 前端UI改進

#### 狀態說明區塊
在課次選擇頁面添加狀態說明：
- 🟡 黃色框框：學習中（尚未完成測驗）
- 🟢 綠色框框：已完成（通過測驗）
- ⚪ 灰色框框：尚未開始學習

#### 測驗狀態檢查
- 顯示課次時檢查測驗狀態
- 根據實際測驗通過情況顯示顏色
- 添加 "⏳ 測驗中" 徽章顯示進行中的測驗

#### 測驗流程改進
- 開始測驗前檢查是否有進行中的測驗
- 提供繼續測驗或重新開始的選項
- 防止重複開始已通過的測驗

### 4. 數據庫遷移

運行 `migrate_quiz_status.py` 腳本：
- 為現有的 `quiz_attempts` 表添加 `status` 字段
- 將現有記錄根據 `completed_at` 字段設置適當的狀態

## 文件修改清單

### 後端文件
- `models.py`：添加 `status` 字段到 `QuizAttempt` 模型
- `app.py`：
  - 修改 `start_quiz()` API
  - 修改 `complete_quiz()` API
  - 添加 `get_quiz_status()` API
  - 修改課程進度邏輯

### 前端文件
- `templates/vocabulary_learning.html`：添加狀態說明區塊
- `static/css/vocabulary_learning.css`：
  - 添加狀態說明樣式
  - 添加進行中測驗徽章樣式
- `static/js/vocabulary_learning.js`：
  - 修改 `displayLessons()` 函數支持測驗狀態檢查
  - 修改 `startQuiz()` 函數添加狀態檢查
  - 所有相關函數改為異步支持

### 工具文件
- `migrate_quiz_status.py`：數據庫遷移腳本
- `test_quiz_status.py`：測試腳本

## 使用說明

### 部署步驟
1. 運行數據庫遷移：`python migrate_quiz_status.py`
2. 重啟應用服務
3. 驗證功能：`python test_quiz_status.py`

### 用戶體驗改進
1. **狀態清晰**：用戶可以清楚看到每個課程的完成狀態
2. **防止數據丟失**：意外離開測驗不會錯誤標記為完成
3. **測驗連續性**：支持繼續未完成的測驗
4. **重複測驗保護**：防止重複開始已通過的測驗

## 測試建議

1. **正常流程測試**：完整完成一個測驗
2. **中斷測試**：測驗進行中刷新頁面，檢查狀態
3. **重複測驗測試**：嘗試重新開始已通過的測驗
4. **狀態顯示測試**：檢查課次頁面的顏色和徽章顯示

## 注意事項

- 現有用戶的測驗記錄會被自動遷移
- 未完成的測驗會被標記為 `abandoned`
- 新的測驗狀態追蹤不會影響現有的學習進度